#include "stdafx.h"
#include "Validate.h"
#include "SystemInfoVisit.h"

Validate::Validate(void)
{
}

Validate::~Validate(void)
{
}
bool Validate::isMatch()
{
	/*SystemInfoVisit systeminfo;
	set<CString> systeminfoset;
	CString systemname;
	systeminfo.getSoftInfo(systeminfoset);
	systemname = systeminfo.getSystemName();
	
	set<CString> * pnoallowSet = g_pconfigmng->getNoallowSet();
	
	set<CString>::iterator itor = pnoallowSet->begin();
	for (;itor != pnoallowSet->end())
	{
		if(systeminfoset.find(*itor) != set::end())
		{
			
		}
	}*/
return true;
}